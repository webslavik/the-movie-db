import { ThemeSwitchDirective } from './theme-switch.directive';

describe('ThemeSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
